"use client";

import { useState } from "react";
import Link from "next/link";

export default function ProviderOnboardingPage() {
  const [submitted, setSubmitted] = useState(false);
  return <main className="hero" style={{ minHeight:"100vh" }}><div className="shell" style={{ maxWidth:760 }}><Link className="brand" href="/"><span className="mark">·</span>homehelp</Link>{submitted ? <div className="card" style={{ padding:30, marginTop:55 }}><div className="kicker">Application received</div><h1 style={{ fontSize:"clamp(2.5rem,7vw,4.8rem)", marginTop:14 }}>You’re on the list.</h1><p className="lead" style={{ fontSize:17 }}>For the Amsterdam pilot, every provider profile is reviewed before it goes live. We’ll contact you with the next steps.</p><Link className="btn btn-primary" href="/" style={{ marginTop:25 }}>Back to HomeHelp</Link></div> : <div className="card" style={{ padding:30, marginTop:55 }}><div className="kicker">Amsterdam pilot / provider application</div><h1 style={{ fontSize:"clamp(2.5rem,7vw,4.8rem)", marginTop:14 }}>Offer your services.</h1><p className="lead" style={{ fontSize:17 }}>Tell us about the work you do, the hours you want, and the rate that works for you.</p><div className="grid grid-2" style={{ marginTop:30 }}><div className="field"><label>First name</label><input className="input" placeholder="Sofia" /></div><div className="field"><label>Last name</label><input className="input" placeholder="van Dijk" /></div><div className="field"><label>Email</label><input className="input" type="email" placeholder="you@example.com" /></div><div className="field"><label>Phone</label><input className="input" placeholder="+31" /></div><div className="field full"><label>Main service</label><select className="select"><option>Cleaning</option><option>Housekeeping</option><option>Electrical</option><option>Plumbing</option><option>Moving</option></select></div><div className="field"><label>Hourly rate (€)</label><input className="input" type="number" defaultValue={24} /></div><div className="field"><label>Years of experience</label><input className="input" type="number" defaultValue={2} /></div><div className="field full"><label>Short introduction</label><input className="input" placeholder="What should customers know about your service?" /></div></div><div className="fee">Pilot policy: HomeHelp takes a 12% platform fee from completed bookings. You choose your availability; profiles are approved before publishing.</div><button className="btn btn-primary" style={{ marginTop:20 }} onClick={() => setSubmitted(true)}>Submit application →</button></div>}</div></main>;
}